# /etc/nginx/conf.d/polycall-upstream.conf
# LibPolycall V2 - Upstream Definitions for Multi-Instance Deployments
#
# For single instance, use the main polycall.conf which defines upstream inline.
# For multi-instance deployments, configure multiple sockets:

# upstream polycall_pool {
#     # Instance 1 (primary)
#     server unix:/var/run/polycall/polycalld-primary.sock weight=5;
#     # Instance 2 (secondary)
#     server unix:/var/run/polycall/polycalld-secondary.sock weight=3;
#     # Instance 3 (edge cache)
#     server unix:/var/run/polycall/polycalld-edge.sock backup;
#
#     keepalive 64;
# }
